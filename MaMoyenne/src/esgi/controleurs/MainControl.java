package esgi.controleurs;

import java.util.ArrayList;
import java.util.List;

import esgi.directmoyenne.MainActivity;


public class MainControl {
	/*private List<User> lu = new ArrayList<User>();
	private List<Admin> ad = new ArrayList<Admin>();
	public MainActivity main = new MainActivity();

		public boolean searchUser(String login, String password){
			 lu = main.datasource.getAllUsers();
			boolean flg = false;
			for(User st : lu){
				if((st.getLoginUser().equals(login)) && (st.getPassword().equals(password)))
					flg = true;
			}
			return flg;
		}

		public boolean searchAdmin(String login, String password){
			ad = main.datasource.getAllAdmin();
			boolean flg = false;
			for(Admin st : ad){
				if((st.getPseudo().equals(login)) && (st.getPass().equals(password)))
					flg = true;
			}
			return flg;
		}*/
		
}
